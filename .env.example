# Backend configuration
# Copy to .env and adjust values as needed

# Server
PORT=4000
NODE_ENV=development

# Database
# Supported: sqlite (default). Placeholders for: postgres, mysql, mssql
DB_DRIVER=sqlite
