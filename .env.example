# Backend configuration
# Copy to .env and adjust values as needed

# Server
PORT=4000
NODE_ENV=development

########################################
# Database
# Supported drivers:
# - sqlite (default, local file db under ./data)
# - pg / supabase (Postgres – recommended for durability)
# - libsql / turso (remote SQLite)

# Set to 'pg' to use Supabase/Postgres locally
# DB_DRIVER=pg

# Supply ONE of the following Postgres connection strings when DB_DRIVER=pg
# Prefer the pooled (non-transactional) connection string from Supabase
# Example: postgres://USER:PASSWORD@HOST:6543/DB_NAME?sslmode=require
# SUPABASE_DB_URL=
# DATABASE_URL=
# PG_CONNECTION_STRING=
# sunbeth_POSTGRES_URL=
# sunbeth_POSTGRES_PRISMA_URL=
# sunbeth_POSTGRES_URL_NON_POOLING=

# For Supabase, SSL is required on pooled connections (default here)
# PGSSLMODE=require

# Default to sqlite when not using external DBs
DB_DRIVER=sqlite

########################################
# Admins for local testing (optional)
# Comma-separated emails; used by some admin endpoints/tests
# REACT_APP_SUPER_ADMINS=you@example.com
