# Local development env for Supabase/Postgres
# Copy to .env (or .env.local) and fill in your values

PORT=4000
NODE_ENV=development

# Enable Postgres adapter
DB_DRIVER=pg

# Supply ONE of these using your Supabase connection string (URI)
# Prefer the pooled (non-transactional) connection string
# Format: postgres://USER:PASSWORD@HOST:6543/DB_NAME?sslmode=require
SUPABASE_DB_URL=
# DATABASE_URL=
# PG_CONNECTION_STRING=
# sunbeth_POSTGRES_URL=
# sunbeth_POSTGRES_PRISMA_URL=
# sunbeth_POSTGRES_URL_NON_POOLING=

# Ensure SSL is on for pooled Supabase connections
PGSSLMODE=require

# Optional: mark a local admin to access admin endpoints and e2e scripts
# REACT_APP_SUPER_ADMINS=you@example.com
